package cn.com.siemens.novel.test;

public class BaiduSearchTest {
    public static void main(String[] args) {
       /* SpiderHandler spiderHandler = new SpiderHandlerInterface();
        spiderHandler.intent.put("key", "免费http代理");
        final long l = System.currentTimeMillis();
        ArrayList<String> list = spiderHandler.handler(SpiderEnum.baiduSearch);
        System.out.println(list);
        for (String url : list) {
            final String ul = url;
            System.out.println(url);
            SpiderHandler spiderHandler2 = new SpiderHandlerInterface();
            spiderHandler2.intent.put(ConfigProperties.URL, ul);

            System.out.println(spiderHandler2.handler(SpiderEnum.catalogue));
            System.out
                    .println("----------------------------------------------------------------------------"
                            + (System.currentTimeMillis() - l));

        }
*/
    }
}
